# SmartSchool updated React app

Run `npm ci` then `npm run dev`, or `npm run build` for production assets. Keep `VITE_USE_MOCKS=false`.

The default application API is `http://localhost:61342`; Identity is `http://localhost:7101`. The backend verification guide provides the matching local Identity launch command and configuration requirements.

Read `VERIFICATION_20260916.md` for completed workflows and the precise verification scope.
